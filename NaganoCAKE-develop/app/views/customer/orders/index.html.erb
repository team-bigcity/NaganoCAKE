<!--% render 'homes/header' -->
  <h2>注文履歴一覧</h2>
  <table class="table">
      <thead class="table-title">
          <tr>
            <th>注文日</th>
            <th>配送先</th>
            <th>注文商品</th>
            <th>支払金額</th>
            <th>ステータス</th>
            <th>注文詳細</th>
          </tr>
      </thead>
      <tbody>
          <% @orders.each do |order| %>
          <tr>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td><%= link_to "表示する", order_path(order), class: "btn btn-primary order_#{order.id}" %></td>
          </tr>
          <% end %>
      </tbody>
  </table>
